#include "main.h"

/**
 *main - function to print abc 10 times
 *
 * Return: Always 0
 */
int main(void)
{
	char c, i;

        for (i = 0; i <= 9; i++)
        {
                for (c = 'a'; c <= 'z'; c++)
	}
                        putchar(c);
			{
                putchar('\n');
        }

    return(0);
}
